class MyIntegerExtras {
	
	/*when writing static classes for MyInteger here are a few different varieties in addition to the one I have. */
	
	/* can also use 
		boolean isEven= v% 2 == 0
		return isEven; 
		
		or

		if (v % 2 == 0){
			return true;
		}
		else{
			return false;
		}	
	*/

	/* can also use 
		return isEven(v.getValue());
	*/
	
}